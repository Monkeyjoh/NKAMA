/**
 * NKAMA — Types de base de données (placeholder).
 *
 * À GÉNÉRER depuis Supabase une fois le projet branché :
 *   npx supabase gen types typescript --project-id <id> > database.types.ts
 *
 * En attendant, on s'appuie sur les typedefs métier de `@/types/domain`.
 * Ce fichier existe pour réserver l'emplacement attendu par l'architecture.
 *
 * @typedef {import("@/types/domain").Property} Property
 * @typedef {import("@/types/domain").Tenant} Tenant
 * @typedef {import("@/types/domain").Lead} Lead
 * @typedef {import("@/types/domain").Contractor} Contractor
 * @typedef {import("@/types/domain").Contract} Contract
 * @typedef {import("@/types/domain").Payment} Payment
 * @typedef {import("@/types/domain").MaintenanceTicket} MaintenanceTicket
 * @typedef {import("@/types/domain").Expense} Expense
 * @typedef {import("@/types/domain").ActivityLog} ActivityLog
 */

export {};
